Git status :- to show the status of git repo.
Git add :- to add repo to staging area.
"Completed Chapter 1"
Git commit -m "message for commit"
Git log :- status of git file
"After adding two more file "
Git add . := to add all the in directory to the staging area 
"Committed at this stage to "two new file is added to directory" "
Git checkout <filename> :- to go back to the perilous version("version control")
Git diff <filename> :- to check the difference between the old file (just after last commit ) and new file


"Time to know how to make remote repository"
Git remote add origin <url>:- for adding remote host 
Git push -u origin master :- push file master branch 
